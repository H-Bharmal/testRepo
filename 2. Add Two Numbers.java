/** * Definition for singly-linked list. * public class ListNode { *     int val; *     ListNode next; *     ListNode() {} *     ListNode(int val) { this.val = val; } *     ListNode(int val, ListNode next) { this.val = val; this.next = next; } * } */class Solution {    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {        int carry=0;        ListNode ans = l1;        while(l1.next != null && l2.next != null){            int val = carry + l1.val + l2.val;            carry = val/10;            l1.val =  val%10;            l1 = l1.next;            l2 = l2.next;        }           // System.out.println(carry);        int temp = carry + l1.val + l2.val;        carry = temp/10;        l1.val =  temp%10;        // System.out.println(l1.val);        //check which is last node        if(l1.next==null && l2.next==null){